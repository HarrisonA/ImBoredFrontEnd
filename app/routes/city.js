// NOTE: This file is connected to the route an individual city

// Leaving the below comments for *reference*, in case you wanted to get
// a list of the has_many relationships (ex: parks, movies, bars)
// Not needed in this app because this info is accessed in the controller

import Ember from 'ember';
//import City from '../models/city';

export default Ember.Route.extend({
  // afterModel() {
  //   var relationshipNames = Ember.get(City, 'relationshipNames');
  //   // console.log("has_many relationships: ", relationshipNames.hasMany);
  // },

});
