<!-- A very quick hack to check display issues: -->
<!-- inline-block to make all divisions line up side by side
So each div (category, actvity list) is NOT on its own line -->
<!-- /*<style media="screen">
  div { display: inline-block;}
</style>*/ -->

<div class="container">
  <div class="jumbotron">

    {{#link-to 'city'}}Back to the {{model.name}} activities list{{/link-to}}
    <h1>Bars in {{model.name}}!!!</h1>
    <button class="btn btn-default">{{#link-to 'city.bar.new'}}Add a new bar {{/link-to}}</button>
    <br><br>

    {{#each model.bars as |bar|}}
          {{activities-list activity=bar}}
    {{/each}}
    <button class="btn btn-default">{{#link-to 'city.bar.new'}}Add a new bar {{/link-to}}</button>

  </div>
</div>
